# Sticklebucket_14 and Sticklebucket_15

For both these scans we had some issues with our 2214 and had to repeat the scanning several times, hence the `proj`, `proj2` and `proj3` folders here.
For both these scans Sheila was able to extract all that was needed from the `proj`/`rec` scans, as can be seen in the Slicer files in the `Analysis` folder.
We thus exclude `rec3` folders from our the reporting notebook (DataWrangling.ipynb) for https://habi.github.io/sticklebacks-manuscript/
